import { StyleSheet } from 'react-native'

export default () =>
  StyleSheet.create({
    backArrow: {
      position: 'absolute',
      top: 20,
      left: 20,
    },
    backArrowMap: {
      position: 'absolute',
      bottom: 20,
      right: 20,
    },
  })
